; Per-directory PHP overrides used by PHP-FPM in production.
; The local dev server (php artisan serve) ignores this file — it reads the
; system php.ini directly.
post_max_size = 50M
upload_max_filesize = 20M
memory_limit = 256M
max_file_uploads = 20
